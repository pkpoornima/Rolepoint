I have built the web app using Python and flask framework. Assuming Python3 and pip3 are installed on the local
environment. Execute following two commands to get the app running.

pip3 install -r requirements.txt
python app.py

Navigate to below url
http://127.0.0.1:5000/